# This workflow is managed by gh actions-lock.

name: GitHub Actions Security Analysis with zizmor

on:
  push:
    branches: ['main']
  pull_request:
    branches: ['**']

permissions: {}

jobs:
  zizmor:
    name: zizmor via PyPI
    runs-on: ubuntu-latest
    permissions:
      security-events: write
      contents: read
      actions: read

    steps:
      - name: Harden the runner (Audit all outbound calls)
        uses: step-security/harden-runner@v2.21.1
        with:
          egress-policy: audit

      - name: Checkout repository
        uses: actions/checkout@v7.0.1
        with:
          persist-credentials: false

      - name: Setup Python
        uses: actions/setup-python@v7.0.0
        with:
          python-version-file: '.python-version'

      # Single source of truth for the zizmor version (Dependabot-managed).
      # Plain (non --user) install so the `zizmor` binary lands on PATH.
      - name: Install zizmor
        run: python3 -m pip install -r requirements-dev.txt

      - name: Run zizmor
        run: zizmor --format=sarif . > results.sarif
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload SARIF file
        uses: github/codeql-action/upload-sarif@v4.38.1
        with:
          sarif_file: results.sarif
          category: zizmor

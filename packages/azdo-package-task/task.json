{
  "id": "a3b5e7f9-2d4c-4f1a-8b6e-0c9d2e3f4a5b",
  "name": "vs-marketplace-package",
  "friendlyName": "VS Marketplace - Package Extension",
  "description": "Package a Visual Studio extension into a .vsix file using VSIXUtil.exe",
  "author": "Jesse Houwing",
  "helpMarkDown": "[More Information](https://github.com/jessehouwing/vs-marketplace)",
  "preview": true,
  "category": "Build",
  "version": {
    "Major": 6,
    "Minor": 0,
    "Patch": 0
  },
  "visibility": ["Build", "Release"],
  "demands": ["visualstudio"],
  "minimumAgentVersion": "2.206.1",
  "groups": [],
  "instanceNameFormat": "Package Visual Studio Extension",
  "inputs": [
    {
      "name": "vsixManifest",
      "type": "filePath",
      "label": "VSIX Manifest file",
      "defaultValue": "",
      "required": true,
      "helpMarkDown": "Path to the `.vsixmanifest` file that describes the extension to package."
    },
    {
      "name": "outputPath",
      "type": "string",
      "label": "Output path",
      "defaultValue": "$(Build.ArtifactStagingDirectory)",
      "required": true,
      "helpMarkDown": "The output path (directory or full `.vsix` file path) for the packaged extension."
    },
    {
      "name": "contentDir",
      "type": "filePath",
      "label": "Content directory",
      "defaultValue": "",
      "required": false,
      "helpMarkDown": "Optional path to the directory containing the extension content files. If omitted, the directory of the manifest file is used."
    }
  ],
  "outputVariables": [
    {
      "name": "vsixFile",
      "description": "Full path to the packaged `.vsix` file."
    }
  ],
  "execution": {
    "Node20_1": {
      "target": "dist/bundle.js",
      "argumentFormat": "",
      "platforms": ["windows"]
    }
  }
}
